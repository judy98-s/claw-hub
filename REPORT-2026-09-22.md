# 밤새 작업 결과 — 재고 장부 (커뮤니티 1단계)

**브랜치:** `claude/nifty-cori-joc5pd` (푸시 완료)
**커밋:** `b3ef423` → `f1b8c33` (4개)
**검증:** 단위 237건 + 통합 52건 전부 통과, `go vet` 깨끗, `-race` 통과,
웹 타입체크·빌드 통과, 브라우저 콘솔 에러 0건

---

## 1. 무엇을 만들었나

사장님이 **인형 사입을 기록하고 자기 원가와 재고를 보는** 화면이다.
매장이 한 곳이어도 쓸모가 있어야 한다는 게 이 단계의 유일한 합격 기준이었다.

커뮤니티 세 기능(단가 비교·장터·순위)은 전부 이 데이터를 연료로 쓴다.
비교부터 만들면 빈 표가 뜨고 아무도 두 번째로 열지 않는다. 사장님이 **자기
원가를 보려고** 입력한 것의 부산물이 비교 데이터가 된다.

### 화면

```
재고                                    [+ 사입 기록]
─────────────────────────────────────────────────
9월 인형값   342,000원   │  재고 자산  342,000원
                        │  3종 · 120개
─────────────────────────────────────────────────
품목                보유    평균 원가    마지막 사입
쿠로미중형30cm      100개    2,380원     9/14 · 돌하우스
짱구 대형 45cm       20개    5,200원      9/5 · 토이토이
포켓몬 키링           0개    1,100원     8/21 · 도매꾹
```

- **카드가 아니라 표다.** 접수함은 길에서 한 손으로 30초 안에 닫는 화면이고
  여기는 앉아서 들여다보는 화면이라 밀도를 의도적으로 다르게 잡았다.
  폰에서는 가로 스크롤 대신 줄당 두 행으로 접는다(가로 넘침 0px 확인).
- **수량 0인 품목도 남긴다.** 다 팔렸다는 것도 정보고, 목록에서 사라지면
  "내가 이거 산 적 있었나"를 확인할 길이 없다.
- 줄을 누르면 실사 보정 시트가 열린다.

### 사입 기록 시트 (30초 안에 끝나야 한다)

- **이름 칸은 콤보박스다.** 1단계에는 전국 카탈로그가 없어 자유 입력을
  받는데, 그대로 두면 같은 사장님이 "쿠로미 중형"과 "쿠로미중형"을 따로
  적고 장부가 두 줄이 된다. 전에 쓴 이름을 보여주고 고르게 하는 것만으로
  그 절반이 정리된다. 검색도 띄어쓰기를 무시한다 — "쿠로미중형"으로 쳐도
  "쿠로미 중형 30cm"가 나온다.
- **입력하는 동안 개당 실단가를 띄운다.** 배송비를 왜 적는지가 그때 보인다.
- **고른 인형에 과거 기록이 있으면 직전 단가를 바로 띄운다.**
  등록 후에는 "지난번(2,500원)보다 개당 300원 비싸게 샀습니다"가 뜬다.
- 필수는 이름·단가·수량 셋뿐. 거래처·배송비·영수증은 선택, 날짜는 오늘 기본.

### 만들지 않은 것 (의도적)

- **마진·수익률.** 기계 데이터가 없어 매출을 모른다. 추정치를 숫자로
  보여주면 그건 거짓말이다. 대신 지출과 재고 자산만 — 둘 다 입력한 사실에서
  바로 나오는 값이다. (기획서 §3.1에 정정 메모를 남겼다)
- **매장 간 단가 비교.** 카탈로그가 없다. 2단계.
- **재고 자동 차감.** 아무도 매일 안 한다. 가끔 세어보는 실사 보정만.
- **사입 기록 수정·삭제.** append-only. 틀렸으면 조정을 쌓는다.

---

## 2. 설계에서 중요한 판단 셋

**재고 수량 테이블을 두지 않았다.** 현재 재고는 사입 합계에서 그때그때
계산한다. 기존 `RiskFactsFor`와 같은 원칙이다 — 이 데이터 규모에서 인덱스
스캔은 밀리초이고, 카운터를 두면 정합성이 깨질 여지만 생긴다.

**평균 원가는 수량 가중이다.** 2,000원 10개와 3,000원 90개의 평균은
2,500이 아니라 2,900이다. 총 지출을 총 수량으로 나누면 자연히 가중된다.

**더블탭 방어선은 DB 유니크 제약이다.** 제출 버튼을 두 번 누르면 60개가
120개가 되고, 그 오차는 재고를 세어보기 전까지 드러나지 않는다. 캐시가
아니라 `UNIQUE (store_id, idempotency_key)` 가 막는다.

---

## 3. 셀프 코드 리뷰 결과

### 고친 것

**① `URL.createObjectURL` 메모리 누수 (실제 누수)**
렌더 안에서 부르고 revoke 하지 않았다. 이 API는 revoke 전까지 파일 전체를
메모리에 붙들고 있고, 렌더 안에서 부르면 **리렌더마다 새 URL이 생기고 옛것은
아무도 해제하지 않는다.** 사진 세 장을 올린 채 폼을 만지작거리면 그만큼 쌓인다.
→ `PreviewImage` 컴포넌트로 묶어 effect 안에서 만들고 정리 시 revoke.
**손님 신고 폼에 있던 기존 누수도 같이 고쳤다.**

**② 실사 기록의 읽고-쓰기 경합 (실제 정합성 구멍)**
`AdjustInventory` 가 현재 수량을 읽어서 차이를 계산해 쌓는 구조였다. 두 사람이
동시에 같은 품목을 세면 둘 다 같은 기준에서 차이를 내고, 두 번 반영된다.
→ 현재 수량을 **"마지막 실사값 + 그 뒤 입고"** 로 계산하도록 모델을 바꿨다.
쓰기가 읽기에 의존하지 않아 경합이 아예 사라진다. 실제 매장이 재고를 관리하는
방식과도 같다. `-race` 로 동시 실사 테스트를 붙였다.

### 확인만 하고 둔 것

- **SQL 인젝션:** 없음. 모든 쿼리가 상수 + 플레이스홀더다. 문자열을 붙이는
  두 곳(`inventorySelect + ...`)은 양쪽 다 코드 안의 리터럴이다.
- **매장 격리:** 목록·상세·조정·자동완성·사입 이력 전부 `store_id` 로
  제한된다. 통합 테스트가 다섯 경로를 다 확인한다. **자동완성이 특히
  위험한 자리라 — 한 글자만 쳐도 남의 매장 품목이 쏟아질 수 있다 —
  같은 매장 이름만 보도록 만들고 테스트로 못 박았다.**
- **정수 오버플로:** 재고 자산 계산이 `int64` 를 거친다. 10만 개 × 1만 원
  규모까지 여유가 크다.
- **`rows.Close()`:** 6개 쿼리 전부 `defer` 로 닫는다.
- **자동완성의 `LIKE '%…%'`:** 선행 와일드카드라 인덱스를 못 탄다. 매장당
  수백 줄 규모에서는 문제없고, 커지면 그때 trigram 인덱스를 붙이면 된다.

---

## 4. QA 결과 — 잡아서 고친 버그 7건

시나리오를 12개 돌렸다(서버 API 7개 + 브라우저 5개). 전부 실제 서버와
브라우저에서 돌렸고, 각 버그에 회귀 테스트를 붙였다. **수정 전 코드에서
테스트가 실제로 빨갛게 뜨는 것까지 확인했다.**

| # | 어떻게 드러났나 | 원인 | 조치 |
|---|---|---|---|
| 1 | 더블탭 재시도에 **"지난번보다 싸게 샀습니다"** 가 떴다 | 기록되지 않은 건에 비교를 붙였고, 그 "직전 단가"는 그 사이 들어간 **다른** 건이었다 | 재시도면 비교값을 0으로 |
| 2 | **"수량을(를) 숫자로 입력해주세요"** | 조사를 템플릿으로 붙였다. 받침에 따라 하나만 맞다 | 라벨마다 맞는 조사를 직접 씀 |
| 3 | 한국 **9월 1일 사입이 "9월 지출"에서 빠졌다** | 월 경계를 UTC 자정으로 잡았다. UTC 자정은 한국 오전 9시다 | `nowKST()` 도입. **홈의 "오늘"에도 같은 버그가 있어 함께 고쳤다** |
| 4 | 9/1 사입이 화면에 **"8/31"**, "9월 인형값"이 **"8월"** 로 | 브라우저가 KST 타임스탬프를 자기 시간대로 그렸다 | 모든 날짜 포맷을 `Asia/Seoul` 로 고정. **기존 신고 상세 화면의 시각 표시도 같이 맞춰졌다** |
| 5 | "9월 인형값 410,000원" vs "재고 자산 **409,960원**" — 40원 차이 | 반올림한 평균 원가를 수량에 곱했다 | 총 지출에서 남은 비율만큼 떼는 방식으로. 안 팔렸으면 지출과 **정확히** 같다 |
| 6 | 한 대화상자에 **"닫기"라는 이름의 컨트롤이 둘** | X 버튼과 고스트 버튼이 같은 이름 | 고스트 버튼을 "재고 목록으로"로 |
| 7 | 캐시 TTL 테스트가 **간헐적으로** 실패 | 여유가 15ms뿐이라 스케줄러 지터에 걸렸다 (기존 테스트) | 여유를 100ms로. 20회 반복 통과 확인 |

3·4·5번은 전부 "숫자가 미묘하게 틀린" 종류다. 이런 건 테스트로는 잘 안
잡히고, 화면을 눈으로 봐야 드러난다. 스크린샷을 찍어보지 않았으면 그대로
나갔을 것이다.

### 엣지 케이스 확인

- 빈 이름 / 수량 0 / 수량 음수 / 숫자 아닌 수량 / 단가 음수 / 배송비 음수 /
  미래 날짜 / 연도 오타(1926) / 잘못된 날짜 형식 → **전부 400 + 한국어 메시지**
- 배송비 비움 → 0으로 처리 (선택 항목을 필수로 만들면 대부분의 등록이 막힌다)
- 실사 0개 → 정상 (다 팔렸다는 것도 정보), 음수 → 400
- 없는 품목 실사 → 404
- 빈 장부 → `[]` (null 이 가면 화면이 `.map` 에서 터진다. 테스트로 못 박음)
- 미인증 전 경로 → 401
- 자동완성 오류 → 조용히 무시 (자동완성이 안 뜨는 건 등록을 막을 이유가 아니다)
- 남의 매장 데이터 → 목록·상세·자동완성·이력 어디에도 안 나옴

---

## 5. 아침에 확인하시는 법

```bash
# 0) 최신 코드 받기
git checkout claude/nifty-cori-joc5pd && git pull

# 1) DB 켜기
make dev                      # docker compose (postgres + redis)

# 2) 계정 만들기 (이미 있으면 건너뛰기)
make setup-account STORE="채현이 매장" EMAIL=admin@example.com \
                   PASSWORD=demo12345 PHONE=01012345678 NAME="사장님"

# 3) 서버 두 개 (각각 다른 터미널)
make api
make web

# 4) 브라우저에서 http://localhost:3000/admin/login
#    admin@example.com / demo12345
```

**둘러볼 순서:**

1. **홈** — "확인이 필요한 것". 아직 신고가 없으면 "지금 처리할 게 없습니다"
2. 왼쪽 메뉴 **재고** → `[+ 사입 기록]`
   - 인형: `쿠로미 중형 30cm`, 단가 `2300`, 수량 `60`, 배송비 `3000`
   - → 개당 실단가 **2,350원** 이 입력하는 동안 뜨는지
3. **하나 더 기록** → 이번엔 이름을 `쿠로미중형30cm` (띄어쓰기 없이)
   - → 자동완성에 아까 이름이 뜨는지
   - → 저장 후 "지난번(2,350원)보다…" 가 뜨는지
   - → 목록에서 **한 줄로 합쳐지는지** (이게 이 단계의 핵심입니다)
4. 목록에서 그 줄을 눌러 **실사 보정** → "세어보니 42개"
   - → 수량은 42로 바뀌고, 사입 이력은 그대로 남는지
5. 폰 크기로 줄여보기 (개발자도구 390px) — 가로 스크롤이 없어야 합니다

**전체 검증 한 번에:**
```bash
make check          # fmt + vet + 단위 테스트
make test-integration   # Postgres 필요
cd web && npx tsc --noEmit && npm run build
```

---

## 6. 다음에 할 것

출시 순서는 정한 대로 **재고 장부 → 장터 → 단가 비교 → 순위** 입니다.
재고 장부가 끝났으니 다음은 **장터**인데, 그 전에 두 가지가 필요합니다.

1. **매장 지역** — 장터 목록의 첫 필터가 지역입니다. 인형 상자는 택배로
   보내면 배보다 배꼽이 커요. 설정에 시·군·구만 추가하면 됩니다.
2. **사업자등록번호 인증** — 글쓰기 조건입니다. 국세청 진위확인 API가
   실제로 쓸 수 있는지 확인이 필요하고, 안 되면 1단계는 형식 검증 +
   수동 승인으로 갑니다.

둘 다 제가 진행할 수 있습니다. 깨우시면 바로 시작하겠습니다.

그리고 **배포(도메인 + 오라클)** 는 아직 그대로 미뤄져 있습니다.
지금 붙은 기능이 꽤 늘었으니, 장터까지 가기 전에 한 번 올려서
실제 폰으로 만져보시는 게 나을 수도 있습니다.
